/**
 * The package name, `@lifi/perps-sdk`.
 *
 * @public
 */
export const name = '@lifi/perps-sdk'

/**
 * The published package version (kept in sync with `package.json` at release).
 *
 * @public
 */
export const version = '0.2.0-alpha.0'
