#!/usr/bin/env node
// Emit Go's vendored `wasm_exec.js` as a TypeScript module that exports the
// `Go` class, then verify the module carries the source verbatim and evaluates.
//
// Usage: node scripts/generate-wasm-exec.mjs

import { mkdirSync, readFileSync, writeFileSync } from 'node:fs'
import { dirname, join } from 'node:path'
import { fileURLToPath, pathToFileURL } from 'node:url'

const packageRoot = dirname(dirname(fileURLToPath(import.meta.url)))
const sourcePath = join(packageRoot, 'wasm', 'wasm_exec.js')
const outputPath = join(
  packageRoot,
  'src',
  'signers',
  'generated',
  'wasmExecRuntime.ts'
)

const source = readFileSync(sourcePath, 'utf8')

// The source is Go-authored JavaScript that the TypeScript checker cannot type;
// it stays verbatim, so the whole file opts out of checking.
const header = `// @ts-nocheck
// Generated by scripts/generate-wasm-exec.mjs from wasm/wasm_exec.js.
// Do not edit: regenerate after vendoring a new Go-built signer runtime.

`

const footer = `
/**
 * Go's WASM runtime class, installed on \`globalThis\` by the source above.
 *
 * @internal
 */
export const Go: new () => {
  importObject: WebAssembly.Imports
  run(instance: WebAssembly.Instance): Promise<void>
} = globalThis.Go
`

mkdirSync(dirname(outputPath), { recursive: true })
writeFileSync(outputPath, header + source + footer)

const emitted = readFileSync(outputPath, 'utf8')
if (emitted !== header + source + footer) {
  console.error(
    `generate-wasm-exec: ${outputPath} does not carry ${sourcePath} verbatim`
  )
  process.exit(1)
}

const { Go } = await import(pathToFileURL(outputPath).href)
if (typeof Go !== 'function' || Go !== globalThis.Go) {
  console.error(
    `generate-wasm-exec: ${outputPath} does not export the Go class installed by wasm_exec.js`
  )
  process.exit(1)
}

console.error(
  `generate-wasm-exec: ${outputPath} carries ${source.length} bytes of wasm_exec.js verbatim and exports Go`
)
