/**
 * Emitted-asset URL of the Go signer binary as resolved by Vite's `?url`
 * pipeline, for the case where its dependency optimizer has relocated this
 * package away from the binary.
 *
 * @internal
 */
export declare const lighterWasmBinaryUrl: URL
