---
"@lifi/perps-sdk-provider-ondo": minor
"@lifi/perps-sdk": patch
---

Ondo answers `getAvailableToTrade` with per-market buy and sell margin from `/v1/perps/max_order_size`, so the client no longer falls back to the account-scoped `availableMargin` for Ondo.
