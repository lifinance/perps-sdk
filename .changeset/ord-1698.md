---
"@lifi/perps-sdk-provider-hyperliquid": patch
---

`getOrder` now sets `explorerLink` on a Hyperliquid order whose placement transaction the explorer still holds, the same as `getOrders`. On both reads, a caller signal that fires during the explorer read now rejects the read instead of returning the orders unlinked.
