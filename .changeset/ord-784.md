---
"@lifi/perps-sdk-provider-lighter": patch
---

Update the vendored Lighter signer wasm to lighter-go main (`c26ac340`) and adapt `LighterSigner` to the new signing ABI. Signing behavior is preserved — integrator fees, self-trade modes, and skip-nonce are all passed as their unset sentinels. Seeded/deterministic `generateAPIKey` is no longer available (the upstream binary dropped seed support); the parameter has been removed.
