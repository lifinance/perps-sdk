---
"@lifi/perps-types": minor
"@lifi/perps-sdk-provider-lighter": minor
---

Add an optional `leverage` multiple to `Fill`, and populate it in the Lighter fill mapper from the venue's pre-trade initial margin fraction.
