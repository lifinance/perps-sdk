---
"@lifi/perps-sdk-provider-lighter": patch
---

Correct the Lighter order wire-type time-unit documentation: `LtOrderBookOrder.transaction_time` no longer claims Unix milliseconds, and `LtOrder.timestamp` now states that its unit is unproven.
