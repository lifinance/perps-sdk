---
"@lifi/perps-sdk-provider-lighter": patch
---

Correct the Lighter order wire-type time-unit documentation: `LtOrderBookOrder.transaction_time` no longer claims Unix milliseconds, `LtOrder.timestamp` now states that its unit is unproven, and the `LtOrder` header now points to the member comments for `timestamp` and `transaction_time` and records that `trigger_time` has no documented unit.
