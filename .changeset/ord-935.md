---
'@lifi/perps-sdk-provider-ondo': patch
---

Fix Ondo live candles appending a new bar per WebSocket tick: `OndoWsProvider.handleKline` now emits the bucket-open time (`kline.s`) as the candle time instead of the per-update timestamp (`kline.t`), so consecutive updates update the forming candle in place.
