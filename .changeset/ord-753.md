---
"@lifi/perps-sdk-provider-hyperliquid": patch
---

Drop stale Hyperliquid compact l2 deltas: a delta whose timestamp is not newer than the maintained orderbook is discarded, so an async-decoded delta that resolves after a newer frame no longer corrupts the book.
