---
'@lifi/perps-types': major
'@lifi/perps-sdk': major
'@lifi/perps-sdk-provider-ondo': minor
---

Replace Ondo venue-side REST execution with API-key HMAC signing.

- **perps-types**: remove `RestCallActionStep`, `RestCallSignedActionStep`, and `SigningMethod.AUTH_TOKEN`; add `SigningMethod.API_KEY`, `ApiKeyRestActionStep`, and `ApiKeyRestSignedActionStep` (the request body is a pre-serialized string that transits verbatim).
- **perps-sdk**: drop the `AUTH_TOKEN` execution detour and the `executeRestCallActions` plugin hook; `API_KEY` steps sign then ride the standard `executeAction` path like EIP-712.
- **perps-sdk-provider-ondo**: remove the venue-side REST execution model; add per-request HMAC-SHA256 signing (`hmacSignRequest`) with a client-held API key minted silently on first trading use, an `OndoApiKeyStore`, and first-login venue-terms acceptance. The JWT and API secret stay userland — only the HMAC signature headers leave the client.
