---
"@lifi/perps-types": minor
"@lifi/perps-sdk-provider-lighter": minor
---

Surface `referralApplied` on `LighterAccountConfig`: `getAccount` reads the applied Lighter referral (SDK-direct, per-user) and `SET_REFERRAL` now projects `satisfied` from it. Configure LI.FI's code via the new `lighterProvider({ referralCode })` option.
