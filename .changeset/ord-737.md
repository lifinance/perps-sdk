---
"@lifi/perps-sdk-provider-lighter": patch
---

Lighter authed reads now reject HTTP 200 responses carrying a non-success body `code`, surfacing a typed `PerpsError` instead of crashing downstream with a `TypeError`.
