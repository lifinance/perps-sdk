---
"@lifi/perps-sdk-provider-lighter": patch
---

Fix `LighterProvider.getOrders` returning self-contradictory pagination: it now reports `hasMore: false` with `limit` equal to the number of orders returned, reflecting that Lighter's `accountActiveOrders` endpoint returns the complete active-order set with no server-side paging.
