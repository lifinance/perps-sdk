---
"@lifi/perps-types": major
"@lifi/perps-sdk": major
"@lifi/perps-sdk-provider-hyperliquid": major
"@lifi/perps-sdk-provider-lighter": major
"@lifi/perps-sdk-provider-ondo": major
---

Ledger transfers now return a resolved `Asset` instead of a display string. Deposits and withdrawals return `Asset | 'USDC'`; only Hyperliquid's fixed-USDC bridge retains the literal. Read `asset.displaySymbol` and `asset.logoURI` after checking that exception.

`Asset.wireId` preserves venue wire identity. `AssetRegistry.get` and `require` accept an optional `id`, `wireId`, or `l1Address` selector. Registry refreshes replace all indexes atomically and reject duplicate identities. Unresolved ledger assets raise an explicit stale or mis-keyed registry error.

Hyperliquid resolves spot token IDs without symbol matching and includes internal, subaccount, and vault deposit/withdrawal transfers. Lighter resolves numeric asset IDs. Ondo resolves venue coin IDs. Hyperliquid and Ondo ledger mapper functions require an asset registry argument. Fee asset strings remain unchanged.

Lighter and Ondo activity cursors with overflow rows use format version `2`. Restart pagination when the provider rejects an older overflow format with `ValidationError`. The provider never treats an old display symbol as an asset identity.

Hyperliquid vault-address deposit feeds omit the depositor identity. Those rows raise a specific `ValidationError`; normal depositor-account vault deposits and withdrawals return complete transfers.
