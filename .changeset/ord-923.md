---
"@lifi/perps-sdk-provider-ondo": patch
---

Fix the Ondo WS provider staying bound to a previous wallet address: releasing all authenticated channels (or a socket drop) now clears the binding and cycles the connection, so a subsequent subscribe for a different address logs in cleanly.
