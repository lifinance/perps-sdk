---
"@lifi/perps-sdk-provider-lighter": patch
---

Lighter `getActivity` now drops a funding or liquidation row whose market the backend market list does not hold, instead of rejecting the whole activity feed.
