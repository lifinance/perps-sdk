---
"@lifi/perps-sdk": patch
---

Fix a reference-data registry refresh race: a slow stale fetch can no longer overwrite a fresher index, and a lookup for a not-yet-seen id now triggers an immediate cache-bypassing refetch instead of being suppressed by an unrelated miss's cooldown, so a just-listed market/asset resolves promptly.
