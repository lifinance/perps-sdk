---
"@lifi/perps-sdk-provider-lighter": patch
"@lifi/perps-types": patch
---

Lighter `account_type` is the account's `SubAccountType`, not an account tier. The account-config projection no longer decodes that integer into a tier, so a sub-account no longer reports the `premium` tier. Without a `user_tier_name` from `/accountLimits` the projected tier is now `null`.
