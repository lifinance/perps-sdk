---
"@lifi/perps-types": minor
---

Add `PerpsErrorCode.Forbidden` (2014) for a caller credential that is valid but lacks the privilege a route requires, so a 403 privilege refusal no longer shares a code with the venue agent rejection `AgentUnauthorized` or the 401 credential rejection `Unauthorized`.
