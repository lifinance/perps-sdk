---
"@lifi/perps-sdk-provider-lighter": patch
---

Correct the `LtOrder` doc comment: `transaction_time` is a Unix microsecond timestamp, not a second one. `created_at` and `updated_at` stay in seconds.
