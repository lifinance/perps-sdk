---
"@lifi/perps-sdk-provider-lighter": minor
---

Record the live Lighter observations that keep six `Lt*` wire types hand-written, and declare `LtAccountPosition.symbol` required. `LtAccountPosition` is a receive-only wire type. The SDK reads it from Lighter's API and no caller constructs it. The required member therefore tightens the read contract and breaks no caller.
