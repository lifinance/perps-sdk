---
"@lifi/perps-sdk": patch
---

Fix `ReconnectingWebSocket.close()` never transitioning connection status to `disconnected`, which left `reconnect()` a no-op, `registerSub` sending into a dead socket, and new status listeners told `connected` after an explicit close.
