---
"@lifi/perps-types": minor
"@lifi/perps-sdk-provider-ondo": minor
---

Add an optional `counterpartyAddress` to `DepositActivity`. The Ondo activity mapper populates it from the deposit's `fromAddress`.
