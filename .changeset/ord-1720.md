---
"@lifi/perps-sdk-provider-lighter": patch
---

Map the Lighter `pending` and `in-progress` order statuses to `PENDING`. The order mapper threw on both, which made the WebSocket handler discard every order in an `account_all_orders` message.
