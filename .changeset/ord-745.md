---
"@lifi/perps-sdk-provider-lighter": patch
---

EVM_TX signing now asserts the wallet is on the backend-specified `txParams.chainId` and throws before broadcasting on a mismatched chain, instead of signing on whatever chain the wallet client is bound to.
