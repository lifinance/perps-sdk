---
"@lifi/perps-sdk-provider-lighter": patch
---

Lighter public signing paths now reject with `PerpsError` and a classified `PerpsErrorCode` instead of a native `Error`, so consumers can inspect the error code.
