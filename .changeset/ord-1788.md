---
"@lifi/perps-sdk-provider-lighter": patch
---

Lighter `getAccount` spot balance rows now carry a `price` and a USD value from the spot market's mark price, and `getAccountSummary` adds every spot row, the settlement row included, to `portfolioValue`.
