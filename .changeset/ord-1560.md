---
"@lifi/perps-sdk": minor
---

`apiKey` is now optional on `PerpsConfig` and `PerpsClientOptions`, and `DEFAULT_API_URL` points at the production host `https://li.quest/v1/perps`, which accepts anonymous requests.
