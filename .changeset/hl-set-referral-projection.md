---
'@lifi/perps-sdk-provider-hyperliquid': patch
---

Project the SET_REFERRAL setup descriptor in the Hyperliquid account-config mapper instead of throwing, fixing getAccount rejecting for every account once the backend serves the setReferrer setup gate
