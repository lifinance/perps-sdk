---
"@lifi/perps-types": minor
---

Add PerpsErrorCode.RateLimitExceeded (2090) for a spent request budget, so clients name the HTTP 429 refusal from the shared enum instead of comparing against a local literal.
