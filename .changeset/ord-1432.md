---
"@lifi/perps-sdk-provider-lighter": patch
---

Correct the `LtLiqTrade.transaction_time` doc comment: Lighter reports the member in microseconds, not milliseconds. Document the unit on `LtDetailedAccount.transaction_time` and `LtSubAccount.transaction_time` as well, because Lighter reports both members in microseconds and neither carried a unit.
