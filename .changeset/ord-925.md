---
"@lifi/perps-sdk-provider-lighter": patch
---

Lighter APPROVE_INTEGRATOR now collects the user's L1 wallet signature and embeds it as `txInfo.L1Sig` before submission, so the venue accepts the integrator approval.
