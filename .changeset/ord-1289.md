---
"@lifi/perps-sdk-provider-hyperliquid": patch
"@lifi/perps-sdk-provider-lighter": patch
"@lifi/perps-sdk-provider-ondo": patch
---

The published `@example` blocks and READMEs no longer show the optional `integrator` field. The API key is the identity the backend resolves.
