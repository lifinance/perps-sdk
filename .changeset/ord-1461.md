---
"@lifi/perps-sdk-provider-lighter": patch
---

Ship Go's `wasm_exec.js` as an ES module that exports the `Go` class, so the Lighter signer loads under a Content Security Policy without `'unsafe-eval'`. `script-src` needs only `'wasm-unsafe-eval'` for `WebAssembly.instantiate`.
