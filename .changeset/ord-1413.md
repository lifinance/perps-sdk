---
"@lifi/perps-sdk-provider-lighter": major
---

Lighter transfer activity now reports its fee on `TransferActivity.fees`, named in the deployment's settlement asset, and no longer carries a `fee` key on `meta`. A consumer that read `meta.fee` reads `fees` instead.
