---
"@lifi/perps-sdk-provider-ondo": patch
"@lifi/perps-sdk-provider-lighter": patch
---

Map an Ondo `pending` order to `PENDING` instead of failing the order read, and map a resting Lighter `pending` trigger order to `OPEN`.
