---
"@lifi/perps-sdk-provider-hyperliquid": patch
---

Give Hyperliquid funding activity rows a deterministic `funding:<coin>:<ISO time>` id instead of the shared zero hash, so consumers can key on `id` without collisions.
