---
"@lifi/perps-sdk-provider-ondo": patch
"@lifi/perps-sdk-provider-lighter": patch
---

Reject an account WebSocket subscription with no auth token on the first call, before it registers a wire subscription that fails on every reconnect.
