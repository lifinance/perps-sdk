---
"@lifi/perps-types": patch
---

The `AccountResponse.collateralBalances` TSDoc now says that an entry is a held collateral balance, which can be a non-quote asset and can be unpriced. It points to `AccountSummary.availableMargin` for buying power.
