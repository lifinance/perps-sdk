---
"@lifi/perps-sdk-provider-lighter": patch
---

Re-create the Lighter standard auth token after `REGISTER_API_KEY` rotates the account API key, and report the venue invalid-signature code on an auth-gated read as an unauthorized error.
