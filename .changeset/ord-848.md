---
"@lifi/perps-sdk-provider-lighter": minor
---

Adopt Lighter's own `'perps'`/`'spot'` route vocabulary for `SEND_ASSET` (the legacy `'perp'` string is no longer accepted) and derive the collateral balance `categoryId` from the fetched markets instead of hardcoding the provider key, with spot token holdings carrying `categoryId: 'spot'`.
