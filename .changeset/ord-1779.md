---
"@lifi/perps-sdk": patch
---

Sign each step of a createAction batch with the signer its own action descriptor declares, so a mixed batch (for example a user-signed builder-fee approval ahead of an SDK-signed order) no longer signs every step with the requested action's signer.
