---
"@lifi/perps-types": patch
---

Correct the `Provider.referralCode` doc-comment. The field records the attribution code that the backend advertises on `/providers` for a venue with a `SET_REFERRAL` setup gate; it no longer claims an authenticated referral-state comparison.
