---
"@lifi/perps-sdk-provider-lighter": minor
---

Lighter read-only token approval now revokes the SDK's own stale registry rows before it creates a replacement, and persists the `token_id` Lighter assigns to each new row.
