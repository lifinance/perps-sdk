---
"@lifi/perps-sdk": patch
---

`getMarket` now throws a `MarketNotFound` `PerpsError` when the backend returns an empty markets list, instead of silently resolving `undefined` under a `Promise<Market>` type.
