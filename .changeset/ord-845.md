---
"@lifi/perps-sdk": minor
---

Encrypt the default `localStorageAdapter` at rest: session secrets are now stored as AES-GCM-256 ciphertext in `localStorage`, keyed by a non-extractable WebCrypto key held in IndexedDB, degrading to no-op writes / null reads when browser crypto storage is unavailable.
