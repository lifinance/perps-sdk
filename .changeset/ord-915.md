---
"@lifi/perps-sdk": minor
---

Add `formatNumber`, a currency-symbol-free display formatter, and a `rounding: 'halfUp' | 'floor'` option on `FormatOptions` so ceiling-validated readouts (available balance, removable margin) can truncate toward zero.
