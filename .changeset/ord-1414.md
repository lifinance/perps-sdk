---
"@lifi/perps-sdk-provider-lighter": patch
---

Report the Lighter fill fee as the amount charged. Lighter publishes each side's fee as a rate on a 1e6 tick scale, so `Fill.fee.amount` is now the trade notional times that rate instead of the raw tick.
