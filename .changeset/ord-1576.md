---
"@lifi/perps-types": minor
---

Add an optional `gatesAccountReads` flag to `ProviderAction`. It marks the setup step whose satisfaction lets a venue authenticate account reads and account streams for the address.
