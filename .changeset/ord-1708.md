---
"@lifi/perps-sdk-provider-lighter": minor
---

Declare `LtAccountPosition.margin_set_flag` required. Live `/api/v1/account` and `account_all_positions` rows carry the member on every position, and the generated `Position` model marks it required. `LtAccountPosition` is a receive-only wire type that the SDK reads from Lighter's API, so the required member tightens the read contract and breaks no caller.
